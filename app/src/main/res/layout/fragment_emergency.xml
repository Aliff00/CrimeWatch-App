<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".EmergencyFragment">

    <!-- RecyclerView to display emergency helpline -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/RVEmergency"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/backgroundGrey" />

    <!--com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/floatingActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_weight="3"
        android:clickable="true"
        android:contentDescription="SOS Call"
        android:focusable="true"
        android:padding="10dp"
        app:backgroundTint="#8b0000"
        app:fabCustomSize="106dp"
        app:maxImageSize="68dp"
        app:shapeAppearanceOverlay="@style/rounded"
        app:srcCompat="@drawable/baseline_sos_24"
        app:tint="#fff2f2" /-->

</LinearLayout>
